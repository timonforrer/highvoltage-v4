---
const {class: className, name, size = 24} = Astro.props;
const { default: innerHTML } = await import(`../icons/${name}.svg?raw`);
---

<svg
  class={className}
  width={size}
  height={size}
  viewBox="0 0 24 24"
  fill="none"
  xmlns="http://www.w3.org/2000/svg">
  <Fragment set:html={innerHTML} />
</svg>